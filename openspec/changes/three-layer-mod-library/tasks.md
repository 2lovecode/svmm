## 1. 本地库存储

- [x] 1.1 增加 `library/` 与 `userdata/` 路径，以及按 UniqueID 存放当前包的目录布局
- [x] 1.2 入库时写入文件清单（相对路径 + 内容哈希）；同一 UniqueID 再次导入替换当前包并重写清单
- [x] 1.3 跳过 SMAPI 自带目录 `ConsoleCommands`、`ErrorHandler`、`SaveBackup`（并用其 manifest UniqueID 复核）

## 2. 安装改道本地库

- [x] 2.1 Nexus 下载、NXM、zip 导入只写入本地库，不改游戏 `Mods/`
- [x] 2.2 为入库、列表、按分类 / UniqueID / 关键词筛选、更新、删除提供命令；删除时从所有方案组移除该 ID，但保留用户数据
- [x] 2.3 列出 Nexus 历史文件并允许降级：下载所选旧文件，替换唯一当前包，不保留旧包
- [x] 2.4 更新或降级后，若该 UniqueID 已在游戏 `Mods/` 中，收取用户数据、替换部署目录、盖回；失败则本地库和该部署目录一起还原。不在 `Mods/` 中则不改游戏目录

## 3. 用户数据与应用事务

- [x] 3.1 实现按清单差异收取用户数据，以及部署后覆盖回写（`config.json` 以玩家文件为准；按 UniqueID 共享）
- [x] 3.2 应用方案组：备份将被删除的玩家模组 → 收取用户数据 → 清空非 SMAPI 模组 → 复制方案组的包 → 盖回用户数据；失败则还原备份
- [x] 3.3 启动时若发现未完成的应用标记，还原备份

## 4. 方案组模型

- [x] 4.1 方案组改为本地库 UniqueID 名单；保证不可删除的默认组；拒绝加入不在本地库的 ID
- [x] 4.2 用 UniqueID 集合加本地库版本判断方案组是否已应用到 `Mods/`
- [x] 4.3 迁移：本地库为空时把现有 `Mods/` 复制入库，启用中的模组进入默认组；旧 `enabledModIds` 能对上的转为新名单

## 5. Nexus 目录状态

- [x] 5.1 浏览支持按分类、Nexus 模组 ID、关键词查询
- [x] 5.2 每条结果对照本地库标记未下载 / 已有 / 有更新；只在游戏 `Mods/` 中存在不算已有

## 6. 界面

- [x] 6.1 首页展示默认方案组，并标明是否已应用到游戏目录
- [x] 6.2 本地库页支持筛选、更新、降级、删除；方案组里该模组展示的版本与本地库当前包一致
- [x] 6.3 方案组页可从本地库添加或移出模组，并执行应用；顶栏切换方案组时执行应用
- [x] 6.4 Nexus 浏览页展示对照本地库的状态，下载动作改为入库

## 7. 验证

- [x] 7.1 为清单、用户数据收取/盖回、应用失败还原、换版本时部署同步与失败还原、降级不留旧包、SMAPI 目录保留补领域测试
- [x] 7.2 走通入库不改 `Mods/`、应用后配置仍在、首页为默认组这三条路径
